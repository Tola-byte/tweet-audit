# io streams: learn io.Reader/io.Writer and io.Copy (30–60m).
# HTTP uploads: r.FormFile, http.MaxBytesReader, ParseMultipartForm and test with curl (30–60m).
# File ops: os.CreateTemp, os.Rename, os.MkdirAll, filepath.Ext (30–60m).
# Concurrency: goroutines + channels and a simple worker loop (1–2h).
# Testing: write a unit test for FileStore.Save to build confidence (1h).